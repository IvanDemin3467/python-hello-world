# Python hello world

Минимальный веб-сервер на стандартной библиотеке Python.

Запуск локально:

```bash
python main.py
```

Порт по умолчанию: `8000`. Можно переопределить через `PORT`.
Любой GET-запрос возвращает `hello world`.

8
